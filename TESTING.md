# Polymarket 持仓助手 — 测试说明

> 按本文从上到下测。**A 部分无需任何持仓**即可验证(加载/设置/认证/刷新/语言/Bug 修复)。**B 部分需要一个小额持仓**才能验证密集流与 dry-run 交易。**C 部分是关掉 dry-run 的真实下单**,务必小额。
>
> ⚠️ **C 部分(实盘下单)目前对「存款钱包(deposit wallet)」账户会失败**(报 `maker address not allowed, please use the deposit wallet flow`)——这是已知的上游 SDK 限制(详见 README「已知限制」),**属预期**,非本扩展 bug。当前重点测 A、B(监控 + 计算 + 模拟);C 留待上游修复或 SDK 迁移后。
>
> 安全前提:此扩展只应用于**只放计划交易资金**的账户;私钥仅本地加密存储。

---

## 0. 准备:加载扩展

1. 终端在项目目录执行构建(已构建可跳过):
   ```bash
   npm install   # 首次
   npm run build
   ```
2. Chrome 打开 `chrome://extensions` → 右上角开「开发者模式」。
3. 「加载已解压的扩展程序」→ 选择 `dist/` 目录。
4. 点扩展图标 → 打开 **Side Panel**(侧边栏)。
5. 调试入口:`chrome://extensions` → 本扩展 → 点 **Service Worker** 链接打开后台控制台(看红色报错用)。改了代码后回到扩展卡片点「↻ 重新加载」,再重开侧边栏。

---

## A. 无需持仓即可测试

### A1. 后台连通 & 面板加载
- 打开侧边栏应正常渲染,无白屏。
- Service Worker 控制台应有 `service worker 已启动`,**无红色报错**。
- ✅ 通过标准:面板出现、顶部标题、设置区、认证区可见。

### A2. 语言切换(中/EN)
- 在设置区(或顶部)找到 **中 / EN** 切换。
- 点切换:整个界面文案应在中/英之间**即时**切换。
- 刷新面板后语言应**保持**(已持久化)。
- ✅ 通过标准:切换即时生效且重开后保留。

### A3. 填写代理钱包地址 → 空持仓态
- 设置里填入你的 Polymarket 主页地址(`polymarket.com/profile/0x...` 里的 `0x` 地址,公开,非私钥)。
- 无持仓时应显示空态提示(不报错、不白屏)。
- 填一个**无持仓的地址**或你的真实地址(当前无仓)均可。
- ✅ 通过标准:保存后顶部徽章变为「监控中」,主体显示空态或持仓列表。

### A4. 手动刷新按钮
- 工具栏点 **↻ 刷新**:应立即重新拉取一次(「最后更新」时间戳刷新)。
- ✅ 通过标准:点按后时间戳更新,无报错。

### A5. 私钥导入 + 会话解锁(用测试小号)
> 建议用一个**只放极少量资金的测试小号**私钥;从 Polymarket 设置 → Private Key 导出,或 reveal.magic.link。
- 认证区无密钥时显示**导入表单**:粘贴私钥 + 设解锁密码(≥8 位)+ 确认密码 → 导入。
- 导入后应显示 **「Unlocked / 已解锁」**,并出现绿色 **「CLOB auth ready / 已就绪」**(说明私钥在后台成功派生了 CLOB API 凭据)。
- 应显示 **Signer 地址(EOA)**,并注明它 ≠ 代理钱包地址(这是正常的)。
- ✅ 通过标准:显示已解锁 + CLOB auth ready + signer 地址。

### A6. 【Bug 回归】解锁输错密码
- 点 **Lock / 锁定**,然后用**错误密码**解锁。
- ✅ 通过标准:应弹出**明确的错误文案**(如「密码错误,或密钥已损坏」),**不再是空白红框**。

### A7. 忘记密钥
- 点 **Forget key / 忘记密钥** → 二次确认 → 确认。
- ✅ 通过标准:回到「无密钥」导入态;`chrome.storage` 中不再有密文(可在 SW 控制台 `chrome.storage.local.get(console.log)` 验证无 `encryptedKey`)。

### A8. 设置持久化 & 护栏默认值
- 确认 **Dry-run 默认开启**(头部/设置头显示**模拟=红**徽章)。
- 改 最大订单 USD、止损最大 USD、主题、**涨跌配色(A股/美股)**、**显示汇总条** → 保存 → 刷新面板应保留。
  - 涨跌配色切「美股」后:盈亏%/盈亏额、YES/NO 徽章、汇总条、进度条应翻转红绿;而模拟徽章/止损/盘口买卖/卖出按钮等固定语义色**不变**。
  - (持仓/盘口轮询间隔已不在 UI;止损滑点已移到每仓止损 Tab。)
- ✅ 通过标准:设置重开后保留;Dry-run 默认为开。

---

## B. 需要一个小额持仓(只读 + dry-run 下单)

> 先在 Polymarket 用**很小金额**买入某个市场(世界杯相关亦可)的少量份额,使账户有 1 个持仓。下面在 **dry-run 开启**下测试,**不会真实下单**。

### B0. 汇总条
- 顶部汇总条显示 **总持仓价值 / 今日(24h)/ 未实现盈亏**(showSummary 开时)。
- ✅ 通过标准:总值≈各持仓现值之和;今日值过几秒/最多 60s 出现(经 user-pnl-api);设置关「显示汇总条」后整条消失。

### B1. 折叠密集流 + 展开
- 持仓按**事件分组**的密集流;每行:YES/NO 徽章、标题、盈亏%、`份额 @ 均价¢ → 现价¢`、持仓价值 · **总盈亏$**(含已实现)。
- 第 3 行是**回本进度条**(双锚点:入场→目标 N×;深亏 >15% 翻转为亏损条);武装止损后该行变 ⛨ 徽章。
- **点击行**手风琴展开操作区(同时只展开一个);价格/盘口/盈亏应**实时跳动**(WebSocket)。
- ✅ 通过标准:数值合理(价值≈份额×现价、盈亏符号正确)、实时更新、单展开、进度条随价动。

### B2. 展开区:回本翻倍价 + 盘口 + 挂单
- **回本翻倍价 chips**:2×/3×/5×(可配),超 $1 标「不可达」;另有蓝色「封盘 N×」chip(=1/均价)。
- **盘口**:默认买1/卖1 + 深度条,点击展开/收起完整 5 档(再点盘口任意处即收起)。
- **挂单**:有未成交单时才显示,可单笔/全部撤销。
- ✅ 通过标准:chips 正确、盘口可展开/收起、无挂单时挂单区隐藏。

### B3. 四种卖出(dry-run,只签名不提交)
- **交易 Tab**:拖「卖出数量」滑块(0–100%,显示份额 + ≈金额);拖「目标倍数 N」非线性滑块(与折叠行进度条**双向联动**,拖到 N×均价>$1 即硬停)。
- 依次点四个按钮:**价格优先**(maker@卖一,被动)、**成交优先**(taker@买一)、**N倍挂单**(GTC@N×均价)、**回收成本**(卖 N×成本/现价)。
- 每次弹**确认弹窗**(后台算好的 价/量/预计金额/订单类型/DRY RUN);确认后结果显示 **`DRY RUN - signed ...`**;**失败时错误显示在弹窗内**(不再像卡住)。
- 越过 最大订单 USD 上限应**报错拦截**。
- ✅ 通过标准:四种都能弹窗 + DRY RUN signed,无真实下单(持仓不变)。

### B4. 自动止损(dry-run)
- **止损 Tab** 4 个滑块:滚动窗口(1–120s)、跌幅阈值(3–50%)、触发卖出比例(5–100%)、**滑点(0–50%)**;点「武装止损」。
- 触发验证:临时把**阈值调到极小**(如 3%)制造触发,测完调回(衡量的是**窗口内从峰值的回撤**,非相对成本浮亏)。
- 触发时弹**桌面通知** `DRY RUN signed`(模拟不真卖);状态格显示 当前跌幅/阈值、上次触发、冷却。
- ✅ 通过标准:dry-run 下触发只通知/签名、**不真卖**;触发后 60s 冷却且需跌幅回落才重新待命。

---

## C. 真实下单 / 真实止损(关 dry-run,务必小额)

> 仅在 A、B 全部通过后再做。**这会动用真实资金且不可逆。**
>
> ⚠️ **当前对「存款钱包」账户会被服务端拒**(`maker address not allowed, please use the deposit wallet flow`)——已知上游 SDK 限制(见 README「已知限制」),非本扩展 bug。下面步骤留待 ① 上游修复 / ② 迁移到 `@polymarket/client` 后再验证;若你是非存款钱包账户(EOA / 经典代理)可正常测。

### C1. 真实小额卖出
- 设置里**关闭 Dry-run**,把 Max order 设成一个**很小**的值(如 5–10 美元)。
- 用「成交优先」卖出**极少量**份额 → 确认弹窗 → 确认。
- ✅ 通过标准:返回 `Submitted ...` 且 Polymarket 上持仓相应减少 / 出现订单;若被拒(如低于最小下单量),应显示**清晰错误**(不再误报成功)。
- ⚠ 若报错,把 Service Worker 控制台的红字贴出来。

### C2. 撤单
- 用「价格优先」挂一个**不会立即成交**的高价卖单 → 在 **Open orders** 看到它 → 点撤销;或一键撤销该持仓挂单。
- ✅ 通过标准:挂单出现并可撤销,撤销后从列表消失。

### C3. 真实止损(可选,谨慎)
- 关 dry-run,武装止损,设合理阈值,等真实瞬跌或小幅调阈值触发一次。
- ✅ 通过标准:触发后真实卖出 n%,桌面通知 `Submitted`,余仓继续持有;后台 60s 冷却生效。

---

## 反馈给我时请附

- 出问题的**步骤编号**(如 B3-成交优先)。
- **Service Worker 控制台**与**面板控制台**(侧边栏右键→检查)的红色报错原文。
- 截图(如展示/数值不对)。

> 已知限制:**存款钱包账户暂不能实盘下单**(上游 SDK 限制,见 README);监控仅在**侧边栏打开**时运行(关掉侧边栏止损暂停);Safari 版后续再做。
